# tasks

- [x] 1. `apps/web/src/app/search/SearchClient.tsx:292` className `font-semibold` → `font-bold` 1건 치환
- [x] 2. `apps/web/src/app/search/SearchClient.tsx:306` className `font-semibold` → `font-bold` 1건 치환
- [x] 3. `apps/web/src/app/search/SearchClient.tsx:344` className `font-semibold` → `font-bold` 1건 치환
- [x] 4. 사후 grep `<h2[^>]*className=` 결과 visible 9건 모두 `font-bold` 보유(ProfileEditForm `text-xl` 사이즈 outlier는 별개 사안) 확인
